1、当有且只有一个客户端连接上去但是没有验证且不发送任何信息则select会一直返回0，不会关闭套接字

2、vector容器在迭代器，如果删除一个值则迭代器失效，需要再给它赋值，我赋值的是容器头部所以会再做一遍循环，加大资源消耗

3、select模型本身限制是轮询模式，开销比较大

在此系统中只允许存在一个验证过的套接字， 另一个验证通过的时候就将上一个套接字关闭，心跳时间为5s, 10s未回复就断开，10s套接字未验证也会断开